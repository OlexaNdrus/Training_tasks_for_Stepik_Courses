import sys,re

for line in sys.stdin:
    line = line.rstrip()

    1
    # matches=re.findall('cat',line)
    # if len(matches)>=2:
    #     print(line)
    2
    # if re.search(r'\bcat\b',line):
    #     print(line)
    3
    # if re.search(r'z.{3}z',line):
    #     print(line)
    4
    # if re.search(r'\\',line):
    #     print(line)
    5
    # p = re.compile(r'((\b\w+)\2)')
    # a=re.search(p,line)
    # p2 = re.compile(r'\w+')
    # b= re.match(p2,line)
    # if a:
    #     if a.group(1) == b.group(0):
    #         print(line)
    6
    # p = re.compile(r'human')
    # print(p.sub('computer',line))
    7
    # p = re.compile(r'\ba+\b',flags=re.IGNORECASE)
    # print(p.sub('argh', line,1))
    8
    # for i in re.findall(r'(\b\w+\b)',line):
    #     # print(re.findall(r'(\b\w+\b)',line))
    #     if len(i)>=2:
    #         # new_str=i[1]+i[0]+i[2:]
    #         # print(re.sub(i,new_str,line))
    9
    # print(re.sub(r'\b(\w)(\w)(\w*)\b', r'\2\1\3', line))




